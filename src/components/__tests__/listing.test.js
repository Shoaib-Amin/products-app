import renderer from 'react-test-renderer';
import Listing from '../Listing';

describe('Listing page', () => {
    test('it renders form when clicked on add new button', () => {
    expect(true).toBe(true);
    });
});